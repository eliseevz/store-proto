import React from "react"
import OrdersList from "../components/ordersList";

const Orders = (props) => {

    return (
        <OrdersList>

        </OrdersList>
    )
}

export default OrdersList

